name: auto_use_case
description: A powerful CLI tool that automatically generates Clean Architecture use cases from repository interfaces. Supports both simple and professional patterns.
version: 1.1.0
repository: https://github.com/Atefaf/use_case_generator
homepage: https://github.com/Atefaf/use_case_generator

environment:
  sdk: '>=3.0.0 <4.0.0'

dependencies:
  path: ^1.8.0

executables:
  auto_use_case: auto_use_case

dev_dependencies:
  test: ^1.21.0

